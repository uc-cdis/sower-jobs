name: Build Docker Images and Push to Quay and ECR

on:
  push:
    paths:
      - 'delete_expired_objects/**'

jobs:
  ci:
    name: Build and Push delete_expired_objects Image
    uses: uc-cdis/.github/.github/workflows/image_build_push.yaml@master
    secrets:
      ECR_AWS_ACCESS_KEY_ID: ${{ secrets.ECR_AWS_ACCESS_KEY_ID }}
      ECR_AWS_SECRET_ACCESS_KEY: ${{ secrets.ECR_AWS_SECRET_ACCESS_KEY }}
      QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
      QUAY_ROBOT_TOKEN: ${{ secrets.QUAY_ROBOT_TOKEN }}
    with:
      DOCKERFILE_LOCATION: ./delete_expired_objects/delete_expired_objects.Dockerfile
      DOCKERFILE_BUILD_CONTEXT: ./delete_expired_objects
      OVERRIDE_REPO_NAME: delete-expired-objects
